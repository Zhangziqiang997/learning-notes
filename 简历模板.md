
王二狗
电话：13312341234 | 邮箱：xxxxxxxxx@qq.com
个人网站：xxxx博客链接/github链接
年龄：25岁 | 性别：男
求职意向：Java后端开发

## 教育经历
xxxx大学 (211)-计算机科学与技术 硕士
2023年09月 - 2026年07月
*   发表xxx相关论文，收录于SCI；读过Clean Code；多次获得奖学金；xxx比赛一等奖。
xxxx大学 (双一流)-计算机科学与技术 本科
2019年09月 - 2023年07月
*   英语CET-6 (550分)；三好学生；担任学习委员。

## 实习经历
xxxxxxx公司-Java开发实习生 牛马部门
2024年03月 - 2024年09月
工作描述：负责xxxx系统的开发，一句话介绍xxx系统的主要功能。自己主要开发了A模块，B模块，C模块。这些模块有什么业务价值。
技术栈：SpringBoot+MySQL+Mybatis+Redis+RabbitMQ+Canal+Elasticsearch
**主要工作：**
*   使用Redis缓存商品详情信息，接口的平均响应耗时从920ms降低到180ms，接口的QPS从xxx提高到xxx
*   使用先更新数据库，监听binlog发送mq异步删除缓存的方案保护缓存和数据库的一致性，使用重试机制确保缓存删除成功
*   基于慢查询日志排查出订单业务的慢SQL，根据执行计划发现没走索引，建立联合索引优化慢SQL，执行耗时从970ms降低到210ms
*   开发模型权重上传功能，由于模型权重较大，基于分片上传+断点续传+秒传实现大文件上传功能
*   开发用户评论功能，基于DFA算法实现敏感词过滤，避免不良发言带来负面影响，降低人工审核成本
*   开发员工签到积分排行榜功能，使用Redis的zset结构实现排序，简单高效；使用定时任务同步每周榜单数据到MySQL，持久化本周排行数据
*   使用Redis的bitmap位图来存储用户签到与补签信息，解决了使用数据库存储签到信息占用太多空间的问题，一人签到一个月只占用4字节
*   对于商品表200w+数据，使用联合索引+子查询分页方案优化深分页问题，执行耗时从1130ms降低到280ms
*   为了避免订单接口重复提交，使用防重Token+Redis保证接口的幂等性，避免出现重复数据
*   开发报表批量导出功能，使用线程池+CompletableFuture+EasyExcel异步批量导出，相比同步方案接口响应耗时从1780ms降低到310ms
*   开发消息通知功能，使用RabbitMQ异步发送通知消息给用户，降低耦合度，接口QPS从xxx提高到了xxx
*   使用消息持久化+消费者ack机制+retry机制，确保RabbitMQ消息可靠性，避免消息丢失问题，将死信队列作为兜底方案
*   使用消息唯一-id+Redis保证RabbitMQ消息的幂等性，避免消息重复消费问题

## 项目经历

**秀柔理发**

项目地址: https://xxxxxxxxxxxxxxxxx
项目描述: 该项目是一个线上理发平台，用户可以在线进行理发预约，上门理发。主要实现了登录注册鉴权，理发优惠券抢购，理发评价推送功能。帮助用户避免理发排队，节省时间。
技术栈: SpringCloud+SpringBoot+MySQL+Mybatis+Redis+RabbitMQ+Canal+Elasticsearch
项目职责:
*   使用Token配合Redis实现分布式会话，解决了集群下Session共享问题。
*   基于Spring Cloud Gateway+Spring Security OAuth2+JWT实现微服务统一认证授权和鉴权，使用RBAC 权限模型实现动态权限控制。
*   自定义缓存工具类，业务中使用缓存时只需调用缓存工具类方法即可避免缓存击穿、穿透、雪崩问题。
*   使用时间戳+序列号结合Redis实现全局分布式id生成器，保证id全局唯一且递增，统计每月、每年的订单量十分方便。
*   使用Redis+Lua实现可重入的分布式锁，避免了集群下JVM锁失效的问题，解决了优惠券的超卖问题，实现了一人一单。
*   使用RabbitMQ优化优惠券下单速度，使用Redis实现库存预扣减，接口的响应耗时由860ms降低为280ms左右。
*   为确保RabbitMQ消息可靠性，开启confirm和return机制以及消费者ack机制和retry机制，将死信队列作为兜底方案。
*   使用RabbitMQ的TTL机制模拟延时队列，实现订单的超时取消。相比于定时任务轮询，极大地减少数据库访问压力。
*   使用Redis实现关注、取关以及共同关注，使用feed流的推模式实现关注推送功能。使用RabbitMQ将推送异步化，大大减少了接口的响应时间。为了避免普通分页读到重复数据，使用滚动分页来获取推送消息。
*   使用Redis的bitmap位图来存储用户签到

## 专业技能

*   具备扎实的Java基础，熟悉集合类原理、多线程、反射、注解、Java8新特性等
*   熟悉MySQL数据库，熟悉索引、事务、锁、MVCC、日志、存储引擎、读写分离、分库分表等；有SQL优化的实战经验
*   Redis实战经验丰富，熟悉分布式会话、分布式锁原理；熟悉缓存击穿、穿透、雪崩的解决方案；熟悉Redis常用数据结构及其原理；熟悉持久化机制，熟悉主从、集群、哨兵等高可用机制
*   RabbitMQ使用经历丰富，熟悉死信队列、延时队列、消息的可靠性原理、消息的幂等性原理、异步、削峰等
*   熟悉JUC，能熟练使用锁、ThreadLocal、线程池、CAS、AQS、各种并发工具类等
*   熟悉JVM内存结构、垃圾回收器、垃圾回收算法、类加载机制、双亲委派原理等，了解JVM调优；能熟练使用Arthas、MAT等工具排查线上问题
*   熟悉Spring Boot、Spring Cloud、MyBatis基本原理与应用，如IOC、AOP、Bean的生命周期、自动装配原理、循环依赖问题、SPI机制、事件驱动、Spring状态机等，能自定义starter
*   熟悉数据结构、计算机网络、操作系统；熟悉TCP/IP协议栈，熟悉TCP可靠性原理、三次握手、四次挥手、流量控制、拥塞控制，熟悉HTTP、HTTPS、DNS等协议
*   熟悉常用设计模式，如单例、工厂、门面、享元、策略等；项目中实际落地使用了很多设计模式
*   熟练使用Linux、Git、Docker、Nginx等工具；能在Linux环境下熟练根据日志排查bug
*   熟悉HTML5, CSS3, JavaScript, Vue, Element UI, Vant, TypeScript等前端知识
*   熟悉AI应用开发工具，熟悉SpringAI、LLM、RAG、Function Call、MCP、LangChain4j等
*   了解深度学习的相关知识，做过CNN, RNN, Transformer, SwinTransformer等神经网络实践

## 荣誉奖项
xxx比赛一等奖
2023年6月
xxx比赛二等奖
2024年4月

## 科研经历

基于Transformer的xxx算法
2024年03月 - 2025年03月
工作描述：一句话介绍这是一个使用了什么技术做的什么东西，解决了什么场景下的什么问题
主要工作：
*   申请了xxx专利/发表了xxx级别的论文，《xxxxxx》，影响因子xxx
*   使用了xxx技术/开发了xxx东西，实现了xxx，某评价指标从xxx提高到了xxx，相比于传统模型/方案，提高了多少

## 个人总结
*   有快速学习的能力，曾花费1周时间学习xxx技术，并立即投入到项目开发中
*   对技术有极大的热情，课余时间自学xx相关技术，并对学习知识记录到xx博客中
*   信息检索能力强，擅长解决疑难杂症，经常使用Google/Github/StackOverflow等解决技术问题
*   动手能力强，有探索精神。经常使用各大厂商的云服务产品，对象存储，云服务器，CDN等
*   善于利用AI工具提高效率，持续关注当前AI技术，自己本地搭建部署xx ai工具
*   擅长阅读英语资料，热衷于看一手英文资料解决问题，经常查看新技术官网和官方文档
